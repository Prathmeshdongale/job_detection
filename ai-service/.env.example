# Path to the directory containing the fine-tuned BERT model and tokenizer
MODEL_PATH=./model/bert_fraud_model

# Attention weight threshold for extracting suspicious phrases (float in [0.0, 1.0])
ATTENTION_THRESHOLD=0.05

# Port the FastAPI server listens on
PORT=8000
